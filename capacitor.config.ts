import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_base_2023',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
